# メンテナンスパス: CC v2.1.157〜161 → v2.1.170 差分の実機検証 + doc 反映

Status: open
Date: 2026-06-10

## 背景

reference docs (distribution/hooks/skills/commands/agents) は **Claude Code v2.1.157〜2.1.161 期**
(2026-05-29〜06-02、コミットログで確定) に執筆。現行 **v2.1.170** との間の changelog 差分のうち
plugin/hook/skill/command/agent に関わるものを実機検証し、検証済みのものから doc 本体へ
`[実機検証済: v2.1.170]` ラベル付きで反映する。

> changelog 文言のまま doc 本体に書かない (= empirical-verification 原則)。実機で挙動を確認してから格上げ。
> 版↔日付は npm publish 時刻で確定 (CHANGELOG に日付欄なし)。

## TODO (執筆後追加 = 未反映の可能性が高い、優先・高)

- [ ] **Stop / SubagentStop の `hookSpecificOutput.additionalContext`** (2.1.163)。hooks.md §7 output schema /
      §10.2 (現状「主に tool 系 + UserPromptSubmit」) / §2.4 Stop 行を更新。
- [ ] **hook `if: "Bash(...)"` のマッチ挙動変更** (2.1.163): `$()` / backtick / `$VAR` を含む全 Bash にマッチ。
      hooks.md §3 `if` field の挙動説明を更新。
- [ ] **`post-session` lifecycle hook** (2.1.169): self-hosted runner 向け、session 終了後・workspace 削除前。
      子プロセス SIGTERM→SIGKILL 猶予 (既定 5s) 設定可。hooks.md event 一覧に追加 (SessionEnd との差を明記)。
- [ ] **skill command body の `\$` エスケープ構文** (2.1.163): 数字前のリテラル `$`。skills.md §4 string substitution に追記。
- [ ] **`/plugin list` (`--enabled`/`--disabled`)** (2.1.163): distribution.md の plugin CLI 節に追加。
- [ ] **`--safe-mode` / `CLAUDE_CODE_SAFE_MODE` / `disableBundledSkills` / `CLAUDE_CODE_DISABLE_BUNDLED_SKILLS`** (2.1.169):
      plugins/skills/hooks 一括無効化。トラブルシュート節候補。
- [ ] **`claude agents --json` の `--all` / `id` / `state` / `waitingFor`** (2.1.162/169): agents.md の JSON スキーマがあれば更新。
- [ ] **Slash command autocomplete: 即実行→入力欄補完** (2.1.162): commands.md の挙動説明があれば修正。

## TODO (執筆"直前" 2.1.152/157 = 当時版に含まれるが反映漏れ要確認、優先・中)

- [ ] **`.claude/skills` plugin の marketplace 不要自動ロード + `claude plugin init`** (2.1.157, 執筆開始と同日)。
      ※CHANGELOG 原文は `.claude/skills` だが `.claude/plugins` の誤記疑い → 実機で配置 dir を検証。
- [ ] **`SessionStart` の `reloadSkills: true` / `sessionTitle`** (2.1.152): hooks.md SessionStart 出力に含まれているか点検。
- [ ] **`MessageDisplay` hook event** (2.1.152): hooks.md §2.8 に既出だが [未検証]、現行で検証可能。
- [ ] **`disallowed-tools` frontmatter (skills/commands)** (2.1.152): skills.md §3 に既出か確認 (記載済みなら OK)。
- [ ] **`/reload-skills`** (2.1.152): skills.md の reload 挙動 (§8) に追記候補。
- [ ] **plugin `defaultEnabled: false`** (2.1.154): distribution.md の marketplace plugin entry に反映確認。

## 該当なし (= 確認済み、doc 変更不要)

- `plugin.json` / `marketplace.json` のスキーマ変更: 範囲内になし。
- `commands/*.md` frontmatter の新設フィールド: なし (`disallowed-tools` は skills 共通)。
- 新 hook matcher / exit code セマンティクス変更: なし。
- `disable-model-invocation` / `user-invocable` の新設・変更: なし (当時すでに存在)。
- subagent 名前空間 / scope の新ルール: なし。

## 完了条件

各 TODO を実機検証 → 該当 reference ファイルへ `[実機検証済: v2.1.170]` 付きで反映 →
SKILL.md 冒頭の「最終検証」スタンプを更新 → 本 issue を削除 (docs-knowledge-flow: 履歴は jj log で追える)。
